$schema: "http://json-schema.org/draft-06/schema#"

description: The cohort-wide analyses that do not explicitly refer to the sample table and use pre-processed merged files are handled by this schema.
