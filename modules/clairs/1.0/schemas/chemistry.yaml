$schema: "http://json-schema.org/draft-06/schema#"

description: Required fields for each sample row in the samples data frame

properties:
    chemistry:
        type: string
        description: The ONT sequencing chemistry used to sequence the sample. All values in this column must appear as keys in CFG["clair3"]["platform"]. 

required:
    - chemistry
