channels:
  - conda-forge
  - bioconda
  - defaults

dependencies:
  - r-base=4.3
  - r-remotes
  - r-optparse
  - r-dplyr
  - r-ggplot2
  - r-tidyr
  - r-data.table
  # GAMBLR.helpers must be installed from GitHub after env creation:
  # Rscript -e 'remotes::install_github("morinlab/GAMBLR.helpers")'
